# OpenSearch + SearXNG for MCP Gateway
# Usage: docker compose -f docker-compose.opensearch.yml up -d

services:
  # Search engine (required for web search)
  searxng:
    image: docker.io/searxng/searxng:latest
    container_name: mcp-searxng
    ports:
      - "8080:8080"
    volumes:
      - searxng-data:/etc/searxng
      - ./searxng-settings.yml:/etc/searxng/settings.yml:ro
      - ./searxng-limiter.toml:/etc/searxng/limiter.toml:ro
    environment:
      - SEARXNG_SECRET=mcp-gateway-secret-key
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3

  opensearch:
    image: opensearchproject/opensearch:2.11.0
    container_name: mcp-opensearch
    environment:
      - discovery.type=single-node
      - plugins.security.disabled=true  # Disable security for local dev
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9200/_cluster/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  opensearch-dashboards:
    image: opensearchproject/opensearch-dashboards:2.11.0
    container_name: mcp-opensearch-dashboards
    environment:
      - OPENSEARCH_HOSTS=["http://opensearch:9200"]
      - DISABLE_SECURITY_DASHBOARDS_PLUGIN=true
    ports:
      - "5601:5601"
    depends_on:
      - opensearch
    restart: unless-stopped

volumes:
  searxng-data:
  opensearch-data:
